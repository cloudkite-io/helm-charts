apiVersion: apps/v1
kind: Deployment
metadata:
  name: vpa-recommender-performance
  namespace: kube-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vpa-recommender-performance
  template:
    metadata:
      labels:
        app: vpa-recommender-performance
    spec:
      serviceAccountName: vpa-recommender
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534 # nobody
      containers:
      - name: recommender
        image: "{{ .Values.recommender.performanceHigh.image.repository }}:{{ .Values.recommender.performanceHigh.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.recommender.performanceHigh.image.pullPolicy }}
        args:
          - --name=performance
          - --target-cpu-percentile=0.95
        resources:
          {{- toYaml .Values.recommender.performanceHigh.resources | nindent 12 }}
        ports:
        - name: prometheus
          containerPort: 8942
