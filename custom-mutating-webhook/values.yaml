replicas: 1

namespace: webhooks
resources: {}
secrets:
  create: false # Indicates if the secret should be created (can be false if the secret already exists)
  tls_crt: ""
  tls_key: ""

tolerations: []

webhook:
  rules:
    - operations: [""]
      apiGroups: [""]
      apiVersions: ["*"]
      resources: [""]
      scope: "*"
  timeoutSeconds: 5

env:
  PATCH_DICT: ""
  MATCH_NAMESPACES: ""
